(function () {
  // 设置计时器
  // 触发计时吕
  // 重置计时器
  // 获取时间
  // 到达时间
  // 范围内时间
  // 超出范围时间
  var ZTime = function () {
    this.prototype = {
      constructor: ZTime;

    };
    return new ZTime();
  };
  var ZTime = new Object();

  // 接口预留
  window.time = ZhengTime = ZTime;
})()
